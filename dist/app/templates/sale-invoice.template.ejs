<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Stock Labels</title>
    <style>
      body {
        font-family: 'Roboto', sans-serif;
        background: #f0f4f8;
        margin: 20px;
        padding: 20px;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
      }
      .label-container {
        width: 320px;
        height: 180px;
        border: 2px dashed #8141D9;
        border-radius: 12px;
        padding: 15px;
        background: #fff;
        box-shadow: 0 4px 10px rgba(129, 65, 217, 0.3);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        margin: 10px;
      }
      .label-header {
        background: linear-gradient(135deg, #8141D9, #9c77dd);
        color: #fff;
        padding: 8px;
        border-radius: 8px;
        text-align: center;
        font-size: 1em; /* Reduced from 1.3em */
        font-weight: bold;
        margin-bottom: 10px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .label-content {
        text-align: center;
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }
      .stock-number {
        font-size: 1.2em; /* Reduced from 1.8em */
        font-weight: bold;
        color: #8141D9;
        margin-bottom: 8px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .purchase-date {
        font-size: 0.9em;
        color: #555;
      }
      .label-footer {
        text-align: center;
        font-size: 0.8em;
        color: #9c77dd;
        border-top: 1px solid #eee;
        padding-top: 8px;
      }
    </style>
  </head>
  <body>
    <% if(data && data.length) { %>
      <% data.forEach(function(label) { %>
        <div class="label-container">
          <div class="label-header"><%= label.item %></div>
          <div class="label-content">
            <div class="stock-number"><%= label.stockNumber %></div>
            <div class="purchase-date">
              Purchased on: <%= label.purchaseDate %>
            </div>
          </div>
          <div class="label-footer">
            Keep this label for inventory tracking
          </div>
        </div>
      <% }); %>
    <% } else { %>
      <p>No stock labels available.</p>
    <% } %>
  </body>
</html>
