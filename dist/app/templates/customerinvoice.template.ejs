<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sale Invoice</title>
    <style>
      body {
        font-family: "Roboto", Arial, sans-serif;
        margin: 0;
        padding: 0;
        background: #f4f7fc;
        color: #333;
      }

      .invoice-container {
        max-width: 900px;
        margin: 30px auto;
        background: #fff;
        border-radius: 10px;
        padding: 20px 30px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      .invoice-header {
        text-align: center;
        border-bottom: 2px solid #f0f0f0;
        padding-bottom: 15px;
        margin-bottom: 20px;
      }

      .invoice-header h1 {
        font-size: 28px;
        color: #4caf50;
        margin: 0;
      }

      .invoice-header p {
        margin: 5px 0;
        font-size: 14px;
        color: #666;
      }

      .details-section {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
      }

      .details-section div {
        width: 48%;
      }

      .details-section h3 {
        font-size: 18px;
        margin-bottom: 10px;
        color: #333;
      }

      .details-section p {
        margin: 4px 0;
        font-size: 14px;
        color: #555;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }

      table th,
      table td {
        border: 1px solid #ddd;
        padding: 10px;
        text-align: left;
        font-size: 14px;
      }

      table th {
        background-color: #4caf50;
        color: white;
        text-transform: uppercase;
      }

      table tr:nth-child(even) {
        background: #f9f9f9;
      }

      table tr:hover {
        background: #f1f1f1;
      }

      .invoice-summary {
        margin-top: 20px;
        text-align: right;
      }

      .invoice-summary h3 {
        font-size: 18px;
        margin: 5px 0;
        color: #333;
      }

      footer {
        text-align: center;
        margin-top: 30px;
        font-size: 12px;
        color: #aaa;
      }

      .btn {
        display: inline-block;
        padding: 8px 16px;
        color: white;
        background: #4caf50;
        border: none;
        border-radius: 5px;
        text-transform: uppercase;
        text-decoration: none;
        font-size: 14px;
        transition: background 0.3s ease;
      }

      .btn:hover {
        background: #45a049;
      }
    </style>
  </head>
  <body>
    <div class="invoice-container">
      <div class="invoice-header">
        <h1>Sale Invoice</h1>
        <p>Business Name</p>
        <p>
          Address JH Hair & Beauty Studio J. P. Arcade, Bhadgaon Road,
          Gadhinglaj, Maharashtra 416502
        </p>
        <p>Mobile: 9607453300 | Email: jawedhabibgad@gmail.com</p>
      </div>

      <div class="details-section">
        <div>
          <h3>Invoice Details</h3>
          <p><strong>Invoice #:</strong> <%= data.txnId %></p>
          <p><strong>Date:</strong> <%= data.txnDate %></p>
        </div>
        <div>
          <h3>Customer Details</h3>
          <p><strong>Name: </strong><%= data.customer %></p>
          <p><strong>Mobile: </strong><%= data.mobile %></p>
        </div>
      </div>

      <table>
        <thead>
          <tr>
            <th>Item Name</th>
            <th>Quantity</th>
            <th>Unit Price</th>
            <th>Tax</th>
            <th>Tax Amount</th>
            <th>Total</th>
          </tr>
        </thead>
        <tbody>
          <% data.itemData.forEach(function(item) { %>
          <tr>
            <td><%= item.name %></td>
            <td><%= item.quantity %></td>
            <td><%= item.unitPrice %></td>
            <td><%= item.taxName %></td>
            <td><%= item.tax %></td>
            <td><%= item.total %></td>
          </tr>
          <% }); %>
        </tbody>
      </table>

      <div class="invoice-summary">
        <h3>Sub Total: <%= data.subTotal %></h3>
        <h3>Total Tax : <%= data.tax %></h3>
        <h3>Total Discount : <%= data.discount %></h3>
        <h3>Grand Total: <%= data.grandTotal %></h3>
      </div>

      <footer>
        <p>Thanks for trusting us with your style! Looking forward to your next visit at JH Hair & Beauty Studio.</p>
      </footer>
    </div>
  </body>
</html>
