<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Happy Birthday!</title>
    <style>
      body {
        font-family: 'Roboto', sans-serif;
        background-color: #f0f4f8;
        color: #555555;
        padding: 20px;
        margin: 0;
      }
      .email-container {
        max-width: 600px;
        margin: auto;
        background-color: #ffffff;
        border-radius: 12px;
        box-shadow: 0 4px 10px rgba(129, 65, 217, 0.3);
        overflow: hidden;
        border: 1px solid #8141d9;
      }
      .header {
        background: linear-gradient(135deg, #8141d9, #9c77dd);
        color: #ffffff;
        padding: 20px;
        text-align: center;
      }
      .header img {
        max-width: 120px;
        margin-bottom: 10px;
      }
      .header h1 {
        margin: 0;
        font-size: 1.8em;
      }
      .content {
        padding: 20px;
        line-height: 1.6;
        text-align: center;
      }
      .birthday-msg {
        font-size: 1.2em;
        color: #8141d9;
        margin-bottom: 20px;
      }
      .coupon {
        background-color: #8141d9;
        color: #ffffff;
        padding: 15px;
        border-radius: 8px;
        font-size: 1.2em;
        font-weight: bold;
        margin: 20px auto;
        width: fit-content;
      }
      .button {
        display: inline-block;
        background-color: #8141d9;
        color: #ffffff;
        padding: 12px 24px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
        margin-top: 20px;
      }
      .footer {
        background-color: #f9f9f9;
        padding: 15px;
        text-align: center;
        color: #9c77dd;
        font-size: 0.9em;
        border-top: 1px solid #eeeeee;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <div class="header">
        <img src="<%= companyLogo %>" alt="<%= companyName %> Logo" />
        <h1>Happy Birthday, <%= customerName %>! 🎉</h1>
      </div>

      <div class="content">
        <p class="birthday-msg">
          Wishing you a fantastic year ahead filled with joy and success!
        </p>

        <p>
          As a token of our appreciation, here’s a special birthday coupon just
          for you:
        </p>

        <div class="coupon"><%= couponCode %></div>

        <p>
          Use this coupon on your next visit. Treat yourself — you deserve it!
        </p>
      </div>

      <div class="footer">
        <%= companyTagline %><br />
        &copy; <%= new Date().getFullYear() %> <%= companyName %>. All rights
        reserved.
      </div>
    </div>
  </body>
</html>
